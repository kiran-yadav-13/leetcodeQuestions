class Solution {
public:
//Brute Force 
    // void markRows(vector<vector<int>> & matrix, int i, int n){
    //     for(int j=0; j<n; j++){
    //         if(matrix[i][j]!=0)
    //         matrix[i][j]=INT_MAX;

    //     }
    // }

    // void markCols(vector<vector<int>>& matrix, int j, int m){
    //     for(int i=0; i<m; i++){
    //         if(matrix[i][j]!=0)
    //         matrix[i][j]=INT_MAX;
    //     }
    // }
    // void setZeroes(vector<vector<int>>& matrix) {
    //     int m=matrix.size();
    //     int n=matrix[0].size();
    //     for(int i=0; i<m; i++){
    //         for(int j=0; j<n; j++){
    //             if(matrix[i][j]==0){
    //                 markRows(matrix, i, n);
    //                 markCols(matrix, j, m);
    //             }
    //         }
    //     }

    //     for(int i=0; i<m; i++){
    //         for(int j=0; j<n; j++){
    //             if(matrix[i][j]==INT_MAX){
    //                 matrix[i][j]=0;
    //             }
    //         }
    //     }
        
    // }
// Better
    // void setZeroes(vector<vector<int>>& matrix) {
    //     int m=matrix.size();
    //     int n=matrix[0].size();

    //     vector<int>rows(m,0);
    //     vector<int>cols(n,0);

    //     for(int i=0; i<m; i++){
    //         for(int j=0; j<n; j++){
    //             if(matrix[i][j]==0){
    //                 rows[i]=1;
    //                 cols[j]=1;
    //             }
    //         }
    //     }
    //     for(int i=0; i<m; i++){
    //         for(int j=0; j<n; j++){
    //             if(rows[i]==1 || cols[j]==1){
    //                 matrix[i][j]=0;
    //             }
    //         }
    //     }
    // }

    //optimal

     void setZeroes(vector<vector<int>>& matrix) {
        //matrix[i][0]--->rows
        //matrix[0][j]--->cols
        int m=matrix.size();
        int n=matrix[0].size();
        int col0=1;
      // Step 1: Mark rows & columns
    for(int i = 0; i < m; i++){
        for(int j = 0; j < n; j++){
            if(matrix[i][j] == 0){
                matrix[i][0] = 0;
                if(j != 0)
                    matrix[0][j] = 0;
                else
                    col0 = 0;
            }
        }
    }

        for(int i=1; i<m; i++){
            for(int j=1; j<n; j++){
                if(matrix[i][j]!=0){
                      if(matrix[0][j]==0 || matrix[i][0]==0){
                    matrix[i][j]=0;
                }
                }
              
            }
        }

          if(matrix[0][0]==0){
            for(int i=0; i<n; i++){
                matrix[0][i]=0;
            }
        }
        if(col0==0){
            for(int i=0; i<m; i++){
                matrix[i][0]=0;
            }
        
     }
        }
      
};
